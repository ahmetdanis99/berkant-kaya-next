export default function AuthContainer({children}:{children:React.ReactNode}) {
    return(
        <div className="min-h-fit h-full w-full mt-5 flex items-center justify-center">{children}</div>
    )
}